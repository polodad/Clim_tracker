{
  "name": "clima-tracker",
  "version": "1.0.0",
  "description": "MVP para monitorear lluvia e inundaciones para flotas en México",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy:prepare": "node scripts/deploy.js",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.8",
    "@types/turf": "^3.5.32",
    "typescript": "^5.2.2",
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "turf": "^3.0.14"
  }
}
